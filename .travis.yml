language: go
language: c

services:
  - docker

env:
  - DOCKER_COMPOSE_VERSION=1.25.0-rc4

before_install:
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin

script:
    # - docker-compose run app sh -c "./app/cTest.sh"
    - docker-compose run app sh -c "go test -mssrv=52.53.245.117 -msdb=northwind -msuser=admin -mspass=Tibco123 -v -run=MS -msdriver="ODBC Driver 17 for SQL Server""
  # - docker-compose run app sh -c "./app/startTest.sh"
